### Github ssh-key 연동방법

ssh key를 만들어 줍니다. 아래 명령어를 수행하면 계정의 Home 디렉토리에 .ssh라는 폴더안에 public, private key가 생성됩니다.

도중에 물어보는 것들은 모두 엔터를 눌러주어서 스킵합니다.

**ssh**-**keygen** -t rsa -b 4096 -C "penguinit0619@gmail.com"

